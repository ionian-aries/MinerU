# MinerU 系统架构与模块拆分说明

本文面向项目接手与后续重构，聚焦当前 `MinerU` 代码结构、核心执行链路、模块边界与优化建议。

## 1. 项目定位与整体设计

MinerU 是一个文档解析平台，支持 `PDF`、图片、`DOCX` 输入，输出 `Markdown`、`content_list`、中间 JSON 等结果。  
系统采用“多入口 + 统一解析编排 + 多后端策略 + 标准输出”的架构：

- **多入口层**：CLI、FastAPI、Router、Gradio
- **编排层**：统一参数、任务控制、后端路由、输出落盘
- **解析后端层**：`pipeline` / `vlm` / `hybrid` / `office`
- **模型与引擎层**：OCR、版面、表格、公式、VLM 引擎适配
- **输出层**：Markdown、JSON、可视化文件、压缩包

## 2. 目录分层与职责拆分

### 2.1 顶层目录职责

- `mineru/cli`：对外入口与服务编排
- `mineru/backend`：后端解析策略与中间结果转换
- `mineru/model`：模型实现与推理引擎封装
- `mineru/utils`：配置、环境、PDF/图像处理、通用工具
- `mineru/data`：文件系统/S3 等数据读写抽象
- `docs`：中英文使用与部署文档
- `tests`：端到端测试与覆盖率脚本

### 2.2 关键目录说明

- `mineru/cli/common.py`：跨入口共享的解析桥接层（核心枢纽）
- `mineru/backend/pipeline`：传统多模型流水线（兼容 CPU）
- `mineru/backend/vlm`：VLM 后端（自动/指定引擎）
- `mineru/backend/hybrid`：VLM + 小模型混合策略
- `mineru/backend/office`：`DOCX` 原生解析

## 3. 对外入口与执行链路

脚本入口定义见 `pyproject.toml`：

- `mineru` -> `mineru.cli.client:main`
- `mineru-api` -> `mineru.cli.fast_api:main`
- `mineru-router` -> `mineru.cli.router:main`
- `mineru-gradio` -> `mineru.cli.gradio_app:main`

### 3.1 CLI 主链路（本地/远端统一）

1. 用户执行 `mineru`
2. `mineru/cli/client.py` 解析输入、分片任务、并发提交
3. 通过 `mineru/cli/api_client.py` 提交至 API
4. 未指定 `--api-url` 时自动拉起本地 `mineru-api`
5. 轮询任务状态，拉取结果并写入输出目录

### 3.2 FastAPI 服务链路

`mineru/cli/fast_api.py` 提供两类接口：

- 异步任务接口：`POST /tasks`、`GET /tasks/{id}`、`GET /tasks/{id}/result`
- 同步兼容接口：`POST /file_parse`

核心特点：

- 使用内存任务管理器维护状态机（`pending/processing/completed/failed`）
- 提供并发信号量控制请求吞吐
- 提供任务保留与后台清理机制

### 3.3 Router 统一入口链路

`mineru/cli/router.py` 用于多实例/多 GPU 调度：

- 对外接口与 `mineru-api` 保持兼容
- 支持本地 worker 拉起 + 上游服务转发混合模式
- 通过 worker 健康状态与负载分数做调度
- 负责任务映射、状态代理、结果透传

### 3.4 Gradio WebUI 链路

`mineru/cli/gradio_app.py` 主要负责：

- 上传与参数配置
- 提交 API 任务并动态展示状态
- 拉取结果、解压并渲染预览
- 本地并发限制与等待队列提示

## 4. 核心数据流（从输入到输出）

### 4.1 输入标准化

入口先统一处理文件类型与命名：

- 后缀识别：`mineru/utils/guess_suffix_or_lang.py`
- 文件读取与图片转 PDF：`mineru/cli/common.py` (`read_fn`)
- 文件名标准化与去重：`normalize_upload_filename`、`uniquify_task_stems`

### 4.2 后端分流

`mineru/cli/common.py` 按输入与后端参数分流：

- `DOCX` -> `mineru/backend/office/docx_analyze.py`
- `PDF/图片` -> `pipeline` / `vlm` / `hybrid`

### 4.3 后端解析

- `pipeline`：`mineru/backend/pipeline/pipeline_analyze.py`
- `vlm`：`mineru/backend/vlm/vlm_analyze.py`
- `hybrid`：`mineru/backend/hybrid/hybrid_analyze.py`

### 4.4 中间结果与最终产物

后端模型输出会转换为统一中间结构，再生成多种产物：

- 中间转换：`model_json_to_middle_json.py`（各后端对应版本）
- 内容生成：`*_middle_json_mkcontent.py`
- 产物类型：
  - `*.md`
  - `*_content_list.json`
  - `*_content_list_v2.json`
  - `*_middle.json`
  - `*_model.json`（可选）
  - 可视化 PDF（layout/span，按开关）

输出路径组织由 `mineru/cli/output_paths.py` 负责。

## 5. 模块依赖与调用关系（重点）

- `cli/client.py` 依赖 `cli/api_client.py`（远程调用）、`cli/output_paths.py`（目录组织）
- `cli/fast_api.py` 依赖 `cli/common.py`（`do_parse` / `aio_do_parse`）
- `cli/router.py` 依赖 `cli/api_client.py` 与本地子进程 worker 管理
- `cli/common.py` 依赖 `backend/*` 与 `utils/*`，是入口到后端的主桥梁
- `backend/*_analyze.py` 依赖 `model/*` 完成推理，再转中间结构
- `utils/config_reader.py` 与 `utils/os_env_config.py` 提供跨模块配置能力

结论：`cli/common.py` 和 `backend/*_analyze.py` 是当前架构最核心、耦合最高的区域。

## 6. 配置体系与环境变量机制

当前采用三层配置叠加：

1. 环境变量（优先级高）
2. 用户配置文件（如 `~/mineru.json`）
3. CLI 参数

核心配置读取在：

- `mineru/utils/config_reader.py`
- `mineru/utils/os_env_config.py`

常见环境变量分组：

- API 并发与输出：`MINERU_API_MAX_CONCURRENT_REQUESTS`、`MINERU_API_OUTPUT_ROOT`
- 任务生命周期：`MINERU_API_TASK_RETENTION_SECONDS`、`MINERU_API_TASK_CLEANUP_INTERVAL_SECONDS`
- Router：`MINERU_ROUTER_UPSTREAM_URLS_JSON`、`MINERU_ROUTER_LOCAL_GPUS`
- 功能开关：公式/表格解析开关、FastAPI 文档开关等

## 7. 并发模型与任务管理

### 7.1 API 侧

- 通过 `asyncio.Semaphore` 控制请求并发
- 内存任务注册表维护任务状态
- 后台清理循环回收过期任务与临时文件

### 7.2 Router 侧

- WorkerPool 周期性健康检查
- 本地 worker 支持按 GPU 自动/手动分配
- 任务转发具备失败重试与降级能力

### 7.3 Client/Gradio 侧

- Client 使用异步队列并发提交任务
- Gradio 通过本地 limiter 限制并发并反馈等待状态

## 8. 后端能力拆分（pipeline / vlm / hybrid / office）

- `pipeline`：轻量兼容、可 CPU 运行，适合资源受限环境
- `vlm-*`：高精度 VLM 路径，支持多推理引擎/远端 API
- `hybrid-*`：结合 VLM 与小模型，平衡精度与效率
- `office`：`DOCX` 原生解析，避免“先转 PDF”带来的精度与速度损失

该设计使系统可根据硬件条件和精度要求进行弹性切换。

## 9. 输出协议与可观测性

输出协议具备良好可消费性：

- 对机器侧：`middle.json`、`content_list.json`
- 对人侧：`md`、可视化 PDF、图片资源目录
- 对服务编排：任务状态与结果 URL

当前可观测能力主要体现在日志与状态接口，指标体系（统一 metrics/tracing）仍有增强空间。

## 10. 测试、发布与部署

- 测试主入口：`tests/unittest/test_e2e.py`
- 覆盖率配置：`pyproject.toml` + `tests/get_coverage.py`
- Docker 部署：`docker/compose.yaml`
- 文档入口：
  - `README_zh-CN.md`
  - `docs/zh/usage/cli_tools.md`
  - `docs/zh/quick_start/docker_deployment.md`

## 11. 技术债与重构机会（优先级）

### P0（优先立即处理）

1. **配置逻辑分散**：`fast_api.py`、`router.py`、`config_reader.py` 存在重复与局部分叉  
   建议：统一为集中 `Settings` 模块（可按域拆分：API/Router/Backend）。
2. **任务状态仅内存保存**：服务重启后任务不可恢复  
   建议：引入可插拔持久层（Redis/DB）与任务恢复机制。

### P1（建议近期处理）

1. **同步/异步路径重复实现**：`do_parse` 与 `aio_do_parse`、后端 analyze 中同构逻辑较多  
   建议：沉淀统一核心流程，I/O 层做同步/异步适配。
2. **`cli/common.py` 职责过重**：输入处理、后端分发、输出写入、环境副作用集中  
   建议：拆分为 `input_normalizer`、`backend_dispatcher`、`output_writer`。
3. **backend 字符串协议分散**：后端标识与引擎标识依赖字符串拼接  
   建议：引入统一枚举与解析器，避免魔法字符串扩散。

### P2（中期优化）

1. **并发策略分散且观测不统一**：Client/API/Router/Gradio 各自限流  
   建议：统一吞吐指标与链路追踪（任务级 trace id + 指标埋点）。
2. **Office 扩展能力不均衡**：`DOCX` 支持完整，`PPTX/XLSX` 仍偏弱  
   建议：插件化扩展 Office 解析器，逐步补齐。

## 12. 建议的接手顺序（实操版）

1. 先跑通 `mineru-api` + `mineru` 单机链路（最小闭环）
2. 理解 `cli/common.py` 的分流与输出控制开关
3. 对比 `pipeline` 与 `vlm/hybrid` 的执行与产物差异
4. 再接入 `mineru-router` 做多实例调度验证
5. 最后推进配置统一与任务持久化改造

---

如需继续，我可以基于这份文档再补一版：

- “重构里程碑路线图（2周 / 1个月 / 1季度）”
- “架构依赖图（Mermaid）”
- “模块负责人交接清单（按文件与风险点）”
