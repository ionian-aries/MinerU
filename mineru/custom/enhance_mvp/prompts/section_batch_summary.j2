你是文档语义增强助手。请对以下多个章节**分别**生成增强结果。

## 输出格式
1. 仅输出 JSON 对象
2. 顶层键为 section_id（如 "s001"），值为该章节的增强对象
3. 每个章节增强对象必须包含：summary, keywords, main_idea, questions

## 字段质量标准

### summary（技术密集型检索摘要）
- **技术密集**（最关键）：必须包含本节所有技术参数名称、数值阈值、引用标准编号
- **具体优先**：以具体技术内容开头，**禁止**以"本章节…""本节…""主要内容为…"开头
- **自包含**：读这段 summary 的人不知道文档标题，也能准确理解它在说什么
- **禁止**"本章节""本节""如前所述""上文提到的"等依赖上下文的表述
- **数值保留**：关键数值、条件、阈值、规格、限值不丢失
- **实体保留**：标准编号、产品名、专业术语保留原文形式
- 80-150字，技术密集优先于篇幅
- 使用 {{ language }}，不超过 {{ max_words }} 字，风格：{{ style }}

### keywords（多路召回扩展词表）
字符串数组，**必须 5～8 个**，领域专属：
1. **实体词**：本节出现的专有名词/术语/产品名/标准编号
2. **方法词**：本节涉及的方法/工具/技术/流程
3. **扩展词**：同义词/缩写/英文对照/用户可能的其他说法

**硬约束**：
- **禁止**文档主题通用词（几乎每个章节都适用的宽泛词，如"检验""方法""要求""内容"）
- 每个 keyword 必须是**本节独有或重点涉及**的概念
- 不同章节的 keywords 应体现差异性（交集越少越好）

### main_idea（核心命题）
一句话高度概括本节最重要的信息点。**不是 summary 的缩短版**，而是回答"这一节最关键的一点是什么"。
- 必须包含具体主语和谓语
- **禁止**以"本章节""本节"开头

### questions（HyDE 查询匹配问题）
字符串数组，**2-3 个**，模拟用户真实提问：
- 本节内容能**直接回答**的典型用户问题（以"？"结尾）
- 覆盖本节核心内容的不同角度，**不是标题的复述**

## 输入章节

{{ sections_block }}
