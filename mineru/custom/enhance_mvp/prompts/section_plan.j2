以下是文档《{{ doc_name }}》的章节结构（共 {{ total_sections }} 个标题节）。

## 大纲说明

- `[NN]` 是标题序号，即输出 seq_ids 中使用的整数
- `（NNN字）` 是该节正文字符数（信息密度参考）
- `> 正文…` 是短正文的内联预览（≤400字）
- `[bN] 文本/表格/图片` 是长正文的逐块展示（>400字时显示），`N` 是块索引，可用于 `split_at`

## 文档层级大纲

{{ outline }}

## 字符数约束

- **chunk_min_chars = {{ chunk_min_chars }}**：正文字符数低于此值的节，信息密度偏低，优先与语义最近的相邻节合并
- **chunk_max_chars = {{ chunk_max_chars }}**：合并后总字符数不得超过此值
  - 若**单节正文**超过此值且正文以 `[bN]` 块形式展示，可在输出中填写 `split_at: [N]` 指定在第 N 块**之前**切分
  - `split_at` 只对 `seq_ids` 为单个元素的节有效；多节合并后不支持 `split_at`
  - 若无需拆分，省略 `split_at` 字段

## 分组规则

1. **字符数约束最高优先**
   - 正文 < chunk_min_chars → 优先合并到语义最近的相邻节
   - 合并后 > chunk_max_chars → 不再合并（可用 split_at 拆分单节超长正文）

2. **语义完整性**
   - 同一主题下的多个子节（如 3.1、3.2、3.3 均属术语定义）可合并为一个单元
   - 问答单元、流程步骤、顺序依赖内容不可拆分

3. **分裂标题识别**（PDF 解析常见问题）
   - 前标题仅为纯数字序号（如"3.2"，字符数≤5），后标题无数字编号前缀，且两者间无正文
   - 此类相邻两个标题必须合并为同一 seq_ids 组

4. **合并只针对连续标题**
   - seq_ids 必须是连续整数（如 [3,4,5]，不得有跳跃）

5. **层级感知合并**
   - 根据标题编号推断深度，相同层级的相邻兄弟节可合并
   - 不得跨越多个层级合并（如"1 范围"和"6.1.1 xxx"不应合并）

## 覆盖约束（必须满足）

- 所有 seq_ids 并集必须恰好等于 {{ seq_id_min | default(0) }} 到 {{ seq_id_max | default((total_sections | int) - 1) }} 的全集
- 每组 seq_ids 内的整数必须连续（如 [0,1,2]，不能有跳跃）
- 单个标题独立成节合法（seq_ids 只含一个元素）
- seq_ids 中的值必须是整数（不是字符串）

## 输出格式

返回 JSON 对象：

```json
{
  "doc_type": "标准|论文|手册|法规|报告|其他",
  "sections": [
    {
      "seq_ids": [0, 1],
      "title": "合并后的单元标题（简洁概括主题）",
      "content_description": "该节内容的自包含一句话概述，可独立理解（RAG检索前缀）",
      "enhance_guidance": "基于本节内容特征的增强指导（1-3句话）",
      "rationale": "分组原因一句话（含字符数依据）",
      "split_at": [3]
    }
  ]
}
```

- `split_at` 可选，值为块索引列表（整数），表示在第 N 块**之前**切分正文
- 其他字段均为必填
