以下是文档《{{ doc_name }}》的标题与正文结构（共 {{ total_sections }} 个标题）。

## 列说明

| 列 | 含义 |
|----|------|
| type | `title[NN]` = 标题（NN 即 seq_ids 中使用的整数）；`body` = 紧跟前一标题的正文 |
| "text/preview" | 标题原文 或 正文预览（首尾截取） |
| chars | 字符数（title = 标题字符数；body = 正文字符数） |
| refs | 物理块引用列表，格式：`页码,块号,类型`，多个用 `; ` 分隔 |

## 文档结构

{{ flat_table }}

## 任务

请根据**语义相关性和内容完整性**，将相邻标题合并为合理的章节单元，用于后续语义增强（摘要、关键词提取、主旨概括）。

## 分组原则

1. **主题紧密关联的相邻标题应合并**
   - 同一主题下的多个子条目（如 3.1、3.2、3.3 均属术语定义）可合并为一个单元
   - 编号层级相近、内容属于同一大主题的短节应合并

2. **正文极少的标题通常应与相邻标题合并**
   - 无 body 行或 chars 很小的标题往往是纯跳转标题，通常应合并

3. **独立完整主题的标题应单独成节**
   - 内容量充足且主题独立的标题（如"4. 技术要求"这类核心章节）保持独立

4. **合并只能针对连续标题，禁止跳跃**
   - seq_ids 必须是连续整数序列（如 [3,4,5]，不能是 [3,5]）

5. **不设硬性字数限制**——以语义关联度为唯一标准

## 输出格式

返回 JSON 对象：

```json
{
  "doc_type": "标准|论文|手册|法规|报告|其他",
  "sections": [
    {
      "seq_ids": [0, 1, 2],
      "title": "合并后的单元标题（简洁概括主题）",
      "rationale": "合并原因（一句话）"
    }
  ]
}
```

## 约束（必须满足）

- sections 中所有 seq_ids 的并集必须恰好等于 0 到 {{ total_sections | int - 1 }} 的全部整数
- 每组 seq_ids 内的整数必须连续（如 [0,1,2]，不能有跳跃）
- 单个标题独立成节也合法（seq_ids 只含一个元素）
- seq_ids 中的值必须是整数（不是字符串）
